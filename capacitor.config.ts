import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'scan-businnesCard',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
